version: "3"
services:
  freshrss:
    image: linuxserver/freshrss
    container_name: freshrss
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/London
    volumes:
      - /home/udocker/freshrss/config:/config
    ports:
      - 8007:80
    restart: unless-stopped